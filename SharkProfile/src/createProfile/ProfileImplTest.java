package createProfile;

//STRG + SHIFT + T zum erzeugen eines Tests auf einer Methode
public class ProfileImplTest {

    //createProfile.CreateProfileImpl profile = new createProfile.CreateProfileImpl();

    /**
    @org.junit.Test
    public void testDivide2numbers() throws Exception {
        double div1 = profile.divide2numbers(12,6);
        //double div2 = profile.divide2numbers(36,6);
        Assert.assertEquals(2.0, div1, 0);
    }

    @org.junit.Test
    public void testMakeSum() throws Exception {
        int sum1 = profile.makeSum(20, 40);
        Assert.assertEquals("20 plus 40 sollte 60 ergeben!", 60, sum1);
    }
    */
}